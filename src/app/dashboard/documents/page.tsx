import { getCurrentUser } from "@/server/auth"; import { prisma } from "@/server/db"; import { DashboardTitle, EmptyRow, Panel, StatusPill } from "@/components/dashboard/DashboardPrimitives"; export const dynamic = "force-dynamic";
export default async function Page() { const user = await getCurrentUser(); const items = user ? await prisma.applicationDocument.findMany({ where: { application: { userId: user.id } }, include: { application: { include: { order: true } } }, orderBy: { createdAt: "desc" } }) : []; return <div className="space-y-8"><DashboardTitle title="Documents" description="Your uploaded ITIN evidence and review status." /><Panel title="Document library">{items.length ? <div className="divide-y divide-border">{items.map((x) => <div key={x.id} className="flex items-center justify-between gap-4 py-4"><div><p className="font-bold">{x.fileName}</p><p className="text-xs text-text-muted">{x.kind} · {x.application.order.reference}</p></div><StatusPill value={x.status} /></div>)}</div> : <EmptyRow message="No documents uploaded yet." />}</Panel></div>; }
