import { z } from "zod";
import { requireAdmin } from "@/server/admin";
import { prisma } from "@/server/db";
import { json,parseJson,routeError } from "@/server/http";
const schema=z.object({decision:z.enum(["APPROVED","REJECTED"]),note:z.string().trim().max(1000).optional()});
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{const actor=await requireAdmin();const {id}=await params;const input=await parseJson(request,schema);const transfer=await prisma.bankTransfer.findUnique({where:{id},include:{checkoutIntent:{include:{order:true}}}});if(!transfer||!transfer.checkoutIntent.order)throw new Error("NOT_FOUND");if(transfer.status!=="PENDING")throw new Error("This payment proof has already been reviewed.");const order=transfer.checkoutIntent.order;await prisma.$transaction(async tx=>{await tx.bankTransfer.update({where:{id},data:{status:input.decision,reviewedById:actor.id,reviewNote:input.note,reviewedAt:new Date()}});if(input.decision==="APPROVED"){await tx.checkoutIntent.update({where:{id:transfer.checkoutIntentId},data:{status:"PAID",paidAt:new Date()}});await tx.order.update({where:{id:order.id},data:{status:"PAID"}});await tx.payment.create({data:{orderId:order.id,provider:"bank transfer",amountCents:order.amountCents,currency:order.currency,status:"PAID"}})}else{await tx.checkoutIntent.update({where:{id:transfer.checkoutIntentId},data:{status:"FAILED"}})}await tx.notification.create({data:{userId:transfer.userId,title:input.decision==="APPROVED"?"Payment approved":"Payment proof needs attention",body:input.decision==="APPROVED"?`Payment for ${order.reference} was approved. You can now start your application.`:(input.note||`Payment proof for ${order.reference} was rejected.`),href:input.decision==="APPROVED"?`/application/${order.id}`:"/dashboard"}});await tx.auditLog.create({data:{actorId:actor.id,action:`Bank payment ${input.decision.toLowerCase()}`,target:order.reference}})});return json({ok:true})}catch(error){return routeError(error)}}
